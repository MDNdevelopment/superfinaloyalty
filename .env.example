# PassKit credentials and endpoints (server-only, read by src/app/api/*).
# Do NOT prefix these with NEXT_PUBLIC_ — they must never reach the browser bundle.
LL_API_KEY=
LL_API_SECRET=
LL_USERNAME=
TERMS_URL=
URL_PROGRAM=
URL_ENROLL=
URL_CHECK=
URL_ADD=
